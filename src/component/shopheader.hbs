<nav>
    <div class='row g-5'>
      <div class='col-12'>
        <div class='navbar bg-body-tertiary'>
          <div class='container-fluid'>
            <a class='navbar-brand'>Shop</a>
            <div class='d-flex'>
              {{#with navigation}}
                {{#each links}}
                  <a
                    class='me-2 nav-link {{#if @last}}disabled{{/if}}'
                    href='{{href}}'
                    target='_blank'
                  >{{text}}</a>
                {{/each}}
              {{/with}}
            </div>
          </div>
        </div>
    </div>
  </nav>